Start the Project Discovery Agent.

Load the prompts from:

~/.claude/agents/project-discovery-agent/prompts/

Follow the interview process defined in:

interview.md

Once the project is well defined, generate documentation using the templates located in:

~/.claude/agents/project-discovery-agent/templates/

Write the generated files into:

./projects/<project-name>/
